# Secure Remote Access Protocol

## Setup
### Server
``python -m project.server.server``

``python -m project.server.server -p 8080`` to run the server on a different port (default 12345)


### Client
``python -m project.client.client``

``python -m project.client.client -p 8080`` to run the client on a different port (default 12345)